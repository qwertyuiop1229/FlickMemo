{
    "name": "flickmemo",
    "version": "1.3.4",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "deploy": "node bump.js && npm run build && git add . && git commit -m \"auto deploy: update code and version\" && git push && firebase deploy"
    },
    "dependencies": {
        "firebase": "^10.12.0",
        "prismjs": "^1.29.0"
    },
    "devDependencies": {
        "vite": "^5.0.0"
    }
}